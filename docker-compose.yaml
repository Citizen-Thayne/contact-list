version: '3.2'

services: 
  api:
    build: api/
    volumes:
      - ./api:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
  web:
    build: web/
    volumes:
      - ./web:/app
    ports:
      - "8001:80"
    depends_on:
      - api
  db:
    image: postgres
    restart: always
    volumes: 
      - contacts-db:/var/lib/postgresql/data

volumes:
  contacts-db: